// repository info
{
  repository(owner: "codesandcoffees", name: "common-utils-pkg") {
    owner {
      login
      url
      avatarUrl
    }
    name
    nameWithOwner
    description
    url
    issues(states: CLOSED) {
      totalCount
    }
    pullRequests(states: OPEN) {
      totalCount
    }
    ... on Repository {
      defaultBranchRef {
        target {
          ... on Commit {
            history(first: 1) {
              nodes {
                message
                committedDate
                committer {
                  email
                  name
                }
                author {
                  email
                  name
                }
              }
            }
          }
        }
      }
    }
  }
}

// user info
{
  user(login: "iamdevlinph") {
    avatarUrl,
    login
  }
}

// org info
{
  organization(login:"codesandcoffees") {
    name,
    url,
    avatarUrl,
  }
}
